spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: jdbc:postgresql://localhost:5433/jira-test
    username: ${DB_PROD_USERNAME:jira}
    password: ${DB_PROD_PASSWORD:JiraRush}